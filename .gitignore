node_modules
dist
.DS_Store
*.local
.env
*.zip
*.log
.vscode/
.idea/
Thumbs.db

# documents / generated PDFs (kept local, not pushed)
*.pdf

# MCP config contains API keys — never commit
.mcp.json
lambda/TransportApi.Tests/bin/
lambda/TransportApi.Tests/obj/
lambda/TransportApi/bin/
lambda/TransportApi/obj/
lambda/VoiceAgent/bin/
lambda/VoiceAgent/obj/

# test run artifacts (regenerated by running the suites; the suites/scripts
# themselves under tests/ are tracked)
test-results/
tests/reports/*.json
tests/reports/*.html
