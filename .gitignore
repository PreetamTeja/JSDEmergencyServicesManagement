node_modules
dist
.DS_Store
*.local
.env
*.zip
*.log
.vscode/
.idea/
Thumbs.db

# documents / generated PDFs (kept local, not pushed)
*.pdf

# MCP config contains API keys — never commit
.mcp.json
lambda/TransportApi.Tests/bin/
lambda/TransportApi.Tests/obj/
lambda/TransportApi/bin/
lambda/TransportApi/obj/
lambda/VoiceAgent/bin/
lambda/VoiceAgent/obj/

# test run artifacts (regenerated by running the suites; the suites/scripts
# themselves under tests/ are tracked)
test-results/
tests/reports/*.json
tests/reports/*.html

# ETL: venv + generated OLAP export (regenerated by running oltp_to_olap.py)
infra/etl/.venv/
infra/etl/olap_export/

# ETL Lambda: build artifacts (zip + generated IAM policy json) regenerated
# by infra/etl_lambda/deploy.sh, plus local lambda invoke() test output
infra/etl_lambda/.build/
infra/etl_lambda/*-out.json
